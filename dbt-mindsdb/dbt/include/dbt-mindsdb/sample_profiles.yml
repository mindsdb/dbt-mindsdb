default:
  target: dev
  outputs:
    dev:
      type: MindsDB(mysql)
      server: [server/host]
      port: [port]  # optional
      database: [schema] # optional
      username: [username]
      password: [password]
      driver: MySQL ODBC 8.0 ANSI Driver
    prod:
      type: MindsDB(mysql)
      server: [server/host]
      port: [port]  # optional
      database: [schema] # optional
      username: [username]
      password: [password]
      driver: MySQL ODBC 8.0 ANSI Driver